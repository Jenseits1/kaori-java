def foo(n: f64): f64;
def bar(n: f64): f64;

def foo(n: f64): f64 {
    if n == 0:
        return;

    bar(n - 1);
}

foo(5);

def bar(n: f64): f64 {
    if n == 0:
        return
    foo(n - 1)
}


